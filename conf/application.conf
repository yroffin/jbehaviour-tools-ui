# This is the main configuration file for the application.
# ~~~~~

# Secret key
# ~~~~~
# The secret key is used to secure cryptographics functions.
# If you deploy your application to several instances be sure to use the same key!
application.secret="7H8H8yr9<dSWgKB7yY0krdThXNXHtnhvlow>w7M/]gy:Se[FKFX/j0@Rg?G_]]Q;"

# The application languages
# ~~~~~
application.langs="en,fr"

# Global object class
# ~~~~~
# Define the Global object class for this application.
# Default to Global in the root package.
# global=Global

# Database configuration
# ~~~~~ 
# You can declare as many datasources as you want.
# By convention, the default datasource is named `default`
#
db.default.driver=org.h2.Driver
db.default.url="jdbc:h2:mem:play"
# db.default.user=sa
# db.default.password=
#
# You can expose this datasource via JNDI if needed (Useful for JPA)
db.default.jndiName=DefaultDS

# Evolutions
# ~~~~~
# You can disable evolutions if needed
# evolutionplugin=disabled

# Ebean configuration
# ~~~~~
# You can declare as many Ebean servers as you want.
# By convention, the default server is named `default`
#
ebean.default="models.*"

# Logger
# ~~~~~
# You can also configure logback (http://logback.qos.ch/), by providing a logger.xml file in the conf directory .

# Root logger:
logger.root=WARN

# Logger used by the framework:
logger.play=WARN

# Logger provided to your application:
logger.application=DEBUG
logger.org.springframework=INFO
org.apache.velocity.app.Velocity=DEBUG

# Spring 
# ~~~~~
# If you don't provide anything Play will look for "application-context.xml" in your conf directory and
# load it. However if you want to direct how spring operates then you may provide additional configuration. 
# By default all options are turned on, so you only need to set something if you need to turn them off or
# use a non-standard name for your application context.

# Defaults to "application-context.xml"
spring.context = application-context.xml

# Defaults to true
spring.namespace-aware = true

# Defaults to true
spring.add-play-properties = true

#
# Custom parameter
#
scm.user = svn
scm.password = "svn"
scm.url = "http://192.168.0.130/svn/projet1/"
scm.tmp = "C:/Users/Yannick/AppData/Local/Temp/jbehaviour-tools"
